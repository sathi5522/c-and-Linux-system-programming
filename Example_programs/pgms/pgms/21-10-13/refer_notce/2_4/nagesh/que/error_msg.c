#include <stdio.h>
#include "header.h"
//#include "error.h"
int error_no;
void error_msg(char *p)
{
	
	if(error_no = FULL)
	{
		printf("%s:que is filled\n",p);
	}
		printf("%s:que is empty\n",p);



}

